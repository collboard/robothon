import { registerModule } from '@collboard/modules-sdk';
import { FunctionBuilderPlugin } from './FunctionBuilderTool';

registerModule(FunctionBuilderPlugin);
//registerModule(FunctionBuilderArtPlugin);
